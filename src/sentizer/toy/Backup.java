package sentizer.toy;

public class Backup {

	
	
}


/* 2015 08 04
for(Pair<String, Double> pair : word2vec.getCloest(query, 3)){
	String wordsub = pair.getFirst();
	Double wordsubScore = pair.getSecond();				
	if( posWordCount.containsKey(wordsub) && negWordCount.containsKey(wordsub)){
		// Naive Bayes
		posScore *= (double)(posWordCount.get(wordsub)+1)/(double)(totalpct+wordSize)*wordsubScore;
		negScore *= (double)(negWordCount.get(wordsub)+1)/(double)(totalnct+wordSize)*wordsubScore;
		// BM25
	}				
}
*/